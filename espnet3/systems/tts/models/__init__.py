"""TTS model architectures for ESPnet3.

Each subpackage holds one architecture and everything specific to it (the
model definition, its backbone modules, and any inference logic it needs).
Architecture-independent parts of the TTS system live one level up, in
``espnet3.systems.tts``.
"""
