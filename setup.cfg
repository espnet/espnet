[tool:pytest]
addopts = --verbose
testpaths = test
python_paths = src/nets src/utils src/bin src/asr src/lm src/tts

[flake8]
ignore = H102
# 120 is a workaround, 79 is good
max-line-length = 120
exclude = src/utils
