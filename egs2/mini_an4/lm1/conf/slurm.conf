# Default configuration
command sbatch --export=PATH
option name=* --job-name $0
option time=* --time $0
option exclude=* --exclude $0
option mem=* --mem-per-cpu $0
option mem=0
option num_threads=* --cpus-per-task $0
option num_threads=1 --cpus-per-task 1
option num_nodes=* --nodes $0
default gpu=0
#default mem=6000
#option gpu=0 -p cpu
option gpu=0 -p swl_general
option gpu=* -p swl_general --gres=gpu:$0 -c $0  # Recommend allocating more CPU than, or equal to the number of GPU
#option gpu=* -p swl_general --gres=gpu:$0 -c $0  # Recommend allocating more CPU than, or equal to the number of GPU
#option gpu=* -p swl_general -w swl-2-13 --gres=gpu:$0 -c $0 #--mem 48000M
