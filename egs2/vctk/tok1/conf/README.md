# Configuration files

Place corpus- and experiment-specific speech-tokenizer training YAML files in
this directory. GAN configurations use `gan_tok_train`; future
non-adversarial configurations use `tok_train`.
