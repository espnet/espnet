# Modified from ESPnet CVSS discrete unit recipe
# https://github.com/espnet/espnet/blob/72dfa2f/egs2/cvss/s2st1/conf/decode_s2st.yaml

batch_size: 1 # Note that we only support batch_size=1 now
seed: 0
maxlenratio: 4
minlenratio: 0.0
threshold: 0.5

use_teacher_forcing: false

beam_size: 1
penalty: 0.0
