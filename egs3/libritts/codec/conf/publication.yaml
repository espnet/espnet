##########################################################
#                  PUBLICATION SETTING                   #
##########################################################
# Quick usage:
#   python run.py --stages pack_model \
#       --training_config conf/training_encodec.yaml \
#       --inference_config conf/inference.yaml \
#       --metrics_config conf/metrics.yaml \
#       --publication_config conf/publication.yaml
#   python run.py --stages upload_model \
#       --training_config conf/training_encodec.yaml \
#       --publication_config conf/publication.yaml
#
# Merged over egs3/TEMPLATE/codec/conf/publication.yaml, which supplies the
# path scaffold, the README template, the exclude list and the upload
# settings. `exp_tag`/`exp_dir` are copied in from the training config by
# run.py, so only recipe-specific keys live here.

pack_model:
  include:
    # `conf/inference.yaml` sets `output_fn: src.inference.build_output`, so
    # the recipe `src` package must travel with the bundle.
    - ${recipe_dir}/src
    - ${recipe_dir}/dataset
