##########################################################
#                    TRAINING SETTING                    #
##########################################################
# Quick usage:
#   python run.py --stages create_dataset --training_config conf/training.yaml
#   python run.py --stages collect_stats  --training_config conf/training.yaml
#   python run.py --stages train          --training_config conf/training.yaml
#
# Main stages and what this config is used for:
# - `create_dataset`: uses `create_dataset`
# - `collect_stats`: uses `dataset`, `dataloader`, `model`, and `parallel`
# - `train`: uses most fields in this file

##########################################################
#                  SYSTEM AND PATH SETTING               #
##########################################################
num_device: 1
num_nodes: 1

# Optional ESPnet task class path used by `get_espnet_model(...)`.
# If unset, `model` is instantiated directly via Hydra.
task:

# Path scaffold relative to the recipe root.
recipe_dir: .
data_dir: ${recipe_dir}/data
exp_tag: ${self_name:}
exp_dir: ${recipe_dir}/exp/${exp_tag}
stats_dir: ${recipe_dir}/exp/stats

# Source corpus location. Keep generated manifests under `${data_dir}`,
# independently of the original audio directory.
dataset_dir: /path/to/your/dataset

##########################################################
#                 DATASET CREATION SETTING               #
##########################################################
# Optional keyword arguments passed to the recipe's `DatasetBuilder` during
# the `create_dataset` stage. The builder is loaded from the dataset reference
# used by this recipe.
#
# If your recipe builds manifests locally, write them under `${data_dir}` so
# later stages can consume the same files without extra path overrides.
create_dataset: {}

##########################################################
#                    DATASET DEFINITION                  #
##########################################################
# Dataset organizer used across collect_stats/train/infer/measure.
# Fill `train`, `valid`, and optionally `test` with dataset reference entries.
#
# Each dataset entry supports:
# - `data_src`: optional dataset source reference.
#   - `<recipe>/<task>` tag, e.g. `voxlingua107/lid`
#   - dotted module path, e.g. `my_project.datasets.foo`
#   - omitted: load `${recipe_dir}/dataset/__init__.py`
# - `data_src_args`: kwargs forwarded to `Dataset(...)`
# - `transform`: optional Hydra-instantiated per-sample transform applied before
#   `preprocessor`
# - `name`: mainly useful for `test`; inference uses it as the output
#   subdirectory name
#
# Only `data_src_args` is passed to the recipe `Dataset` constructor.
# Top-level fields such as `name` and `transform` stay in organizer space.
#
# Example:
# dataset:
#   _target_: espnet3.components.data.data_organizer.DataOrganizer
#   recipe_dir: ${recipe_dir}
#   train:
#     - data_src: voxlingua107/lid
#       data_src_args:
#         split: train
#         source_dir: ${dataset_dir}
#   valid:
#     - data_src_args:
#         split: dev
#         source_dir: ${dataset_dir}
#   test:
#     - name: dev
#       data_src_args:
#         split: dev
#         source_dir: ${dataset_dir}
#   preprocessor:
#     _target_: espnet2.train.preprocessor.LIDPreprocessor
dataset:
  _target_: espnet3.components.data.data_organizer.DataOrganizer
  _recursive_: false
  recipe_dir: ${recipe_dir}
  train:
  valid:
  test:
  preprocessor:

##########################################################
#                      MODEL SETTING                     #
##########################################################
# Training model definition.
# If `task` is set, this block is passed to `get_espnet_model(task, model_conf)`.
# Thus, you can reuse an ESPnet2-style LID model config by setting `task` and
# filling this block with the per-model configuration.
# If `task` is not set, this block is instantiated directly via Hydra.
model:

##########################################################
#            OPTIMIZER & SCHEDULER SETTING               #
##########################################################
# Single optimizer path:
optimizer:
  _target_: torch.optim.Adam
  lr: 0.000005
  betas: [0.9, 0.98]

scheduler:
  _target_: espnet2.schedulers.tristage_lr.TristageLR
  # ESPnet2 baseline value, measured in optimizer updates, not input batches.
  # VoxLingua107's 30 * 1000 batches / accumulation 2 give only 15000 updates.
  # Use 15000 for a full three-stage schedule; this changes the baseline.
  max_steps: 30000
  warmup_ratio: 0.3
  hold_ratio: 0.2
  decay_ratio: 0.5
  init_lr_scale: 0.6
  final_lr_scale: 0.1

# Used only in the single optimizer path.
scheduler_interval: step   # `step` or `epoch`
scheduler_monitor:         # e.g. `valid/loss` for monitored epoch schedulers

##########################################################
#                OTHER TRAINING SETTING                  #
##########################################################
# Checkpoint selection criteria passed to ESPnet3 callbacks.
# Each item is:
#   - monitor key
#   - number of best checkpoints to keep
#   - direction (`min` or `max`)
best_model_criterion:
  - - valid/accuracy
    - 2
    - max

# Optional random seed used by collect_stats/train.
seed:

# Extra model initialization mode forwarded to ESPnet's initializer.
# Example: xavier_uniform
init:

##########################################################
#                   PARALLELIZATION SETTING              #
##########################################################
# Parallel backend used by collect_stats/train.
parallel:
  env: local
  n_workers: 1

##########################################################
#                   DATALOADER SETTING                   #
##########################################################
dataloader:
  collate_fn:
    _target_: espnet2.train.collate_fn.CommonCollateFn
    int_pad_value: -1
    not_sequence:
      - lid_labels
  # Training dataloader / iterator settings.
  train:
    # `total_shards` is the total number of dataset shards.
    # `dist_world_size` is the distributed world size used to rotate shards.
    # Keep `dist_world_size` equal to the runtime world size.
    total_shards: 1
    dist_world_size: 1
    iter_factory:
      _target_: espnet2.iterators.sequence_iter_factory.SequenceIterFactory
      # Integer seed for the iterator; set `${seed}` when the recipe sets a seed.
      seed: 0
      shuffle: true
      collate_fn: ${dataloader.collate_fn}
      batches:
        # Typical modes include `unsorted`, `sorted`, and LID-specific
        # `catbel`, `catpow`, or `catpow_balance_dataset`.
        type: unsorted
        shape_files:
          - ${stats_dir}/train/speech_shape
        batch_size: 4
        batch_bins: 2880000
  # Validation dataloader / iterator settings.
  valid:
    # Use the same sharding settings as `train`.
    total_shards: 1
    dist_world_size: 1
    iter_factory:
      _target_: espnet2.iterators.sequence_iter_factory.SequenceIterFactory
      seed: ${dataloader.train.iter_factory.seed}
      shuffle: false
      collate_fn: ${dataloader.collate_fn}
      batches:
        type: ${dataloader.train.iter_factory.batches.type}
        shape_files:
          - ${stats_dir}/valid/speech_shape
        batch_size: ${dataloader.train.iter_factory.batches.batch_size}
        batch_bins: ${dataloader.train.iter_factory.batches.batch_bins}

##########################################################
#                    TRAINER SETTING                     #
##########################################################
# These settings map to `lightning.Trainer(...)`.
trainer:
  accelerator: auto
  devices: ${num_device}
  num_nodes: ${num_nodes}
  accumulate_grad_batches: 1
  check_val_every_n_epoch: 1
  gradient_clip_val: 1.0
  log_every_n_steps: 100
  max_epochs: 30

  logger:
    - _target_: lightning.pytorch.loggers.TensorBoardLogger
      save_dir: ${exp_dir}/tensorboard
      name: tb_logger

  strategy: auto

##########################################################
#                      FIT SETTING                       #
##########################################################
# Optional keyword arguments passed directly to `trainer.fit(**fit)`.
# Leave `{}` for the default flow.
fit: {}
