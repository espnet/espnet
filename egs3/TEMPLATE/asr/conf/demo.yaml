system: base
infer_config: conf/infer.yaml

output_keys:
  text: hyp

# Inference runner kwargs (input_key/output_fn_path) are derived from infer_config.

pack:
  out_dir: demo
  files:
    - exp/your_exp_dir/last.ckpt  # override in recipe
    - exp/your_exp_dir/config.yaml

upload_demo:
  hf_username: your-hf-username
  repo_name: your-demo
  repo_type: space
  space_sdk: gradio
  # organization: your-org
