pack_model:
  strategy: auto  # auto|espnet2|espnet3
  out_dir: exp/model_pack
  decode_dir: ${exp_dir}/decode
  include: []
  extra:
    - ${load_yaml:training.yaml,tokenizer.save_path}
  include_data_dir: false
  exclude:
    - "**/*.log"
    - "**/tensorboard/**"
    - "**/wandb/**"
  files: {}
  yaml_files: {}
  espnet2:
    task: asr
    yaml_files: {}
    files: {}
    option: []

upload_model:
  hf_repo: yourname/your-model-repo
