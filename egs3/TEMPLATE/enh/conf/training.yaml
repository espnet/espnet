##########################################################
#                    TRAINING SETTING                    #
##########################################################
# Quick usage:
#   python run.py --stages create_dataset --training_config conf/training.yaml
#   python run.py --stages collect_stats  --training_config conf/training.yaml
#   python run.py --stages train          --training_config conf/training.yaml
#
# Main stages and what this config is used for:
# - `create_dataset`: uses `create_dataset` / the recipe `DatasetBuilder`
# - `collect_stats`: uses `dataset`, `dataloader`, `model`, and `parallel`
# - `train`: uses most fields in this file
#
# Speech enhancement has no tokenizer stage.

##########################################################
#                  SYSTEM AND PATH SETTING               #
##########################################################
num_device: 1
num_nodes: 1

# Optional ESPnet task class path used by `get_espnet_model(...)`.
# If unset, `model` is instantiated directly via Hydra.
task: espnet3.systems.enh.task.EnhancementTask

# Path scaffold relative to the recipe root.
recipe_dir: .
data_dir: ${recipe_dir}/data
exp_tag: ${self_name:}
exp_dir: ${recipe_dir}/exp/${exp_tag}
stats_dir: ${recipe_dir}/exp/stats

# Directory where this recipe stores prepared manifests and any recipe-local
# dataset artifacts, or the external corpus root for datasets that read wavs
# directly (e.g. VCTK-DEMAND).
#
# Keep this value as the single source of truth for dataset location. Training,
# inference, and dataset creation should all reuse the same path instead of
# redefining dataset names in multiple configs.
dataset_dir: /path/to/your/dataset

##########################################################
#                 DATASET CREATION SETTING               #
##########################################################
# Optional kwargs forwarded to `DatasetBuilder.prepare_source` / `build`.
# If your recipe builds manifests locally, write them under `${dataset_dir}` so
# later stages can consume the same files without extra path overrides.
create_dataset:
  recipe_dir: ${recipe_dir}
  dataset_dir: ${dataset_dir}

##########################################################
#                    DATASET DEFINITION                  #
##########################################################
# Dataset organizer used across collect_stats/train/infer/measure.
# Fill `train`, `valid`, and optionally `test` with dataset reference entries.
#
# Each dataset entry supports:
# - `data_src`: optional dataset source reference.
#   - `<recipe>/<task>` tag, e.g. `mini_an4/enh`
#   - dotted module path
#   - omitted: load `${recipe_dir}/dataset/__init__.py`
# - `data_src_args`: kwargs forwarded to `Dataset(...)`
# - `transform`: optional Hydra-instantiated per-sample transform
# - `name`: mainly useful for `test`; inference uses it as the output
#   subdirectory name
#
# Example:
# dataset:
#   train:
#     - data_src_args:
#         split: train
#         data_path: ${dataset_dir}
#   valid:
#     - data_src_args:
#         split: valid
#         data_path: ${dataset_dir}
#   preprocessor:
#     _target_: espnet2.train.preprocessor.EnhPreprocessor
#     speech_ref_name_prefix: speech_ref
#     num_spk: 1
#     sample_rate: 16000
dataset:
  _target_: espnet3.components.data.data_organizer.DataOrganizer
  _recursive_: false
  recipe_dir: ${recipe_dir}
  train:
  valid:
  test:
  preprocessor:

##########################################################
#                      MODEL SETTING                     #
##########################################################
# Training model definition for `EnhancementTask`.
# Fill encoder / separator / decoder / criterions (or model_conf.loss_type)
# in the concrete recipe override.
model:

##########################################################
#                   OPTIMIZER / SCHEDULER                #
##########################################################
optimizer:
  _target_: torch.optim.Adam
  lr: 1.0e-03
  eps: 1.0e-08
  weight_decay: 1.0e-05

scheduler:
  _target_: torch.optim.lr_scheduler.ReduceLROnPlateau
  mode: min
  factor: 0.5
  patience: 3

scheduler_interval: epoch
scheduler_monitor: valid/loss

init: xavier_uniform

best_model_criterion:
  - - valid/loss
    - 1
    - min

##########################################################
#                  PARALLELIZATION SETTING               #
##########################################################
parallel:
  env: local
  n_workers: 1

##########################################################
#                   DATALOADER SETTING                   #
##########################################################
dataloader:
  collate_fn:
    _target_: espnet2.train.collate_fn.CommonCollateFn
    int_pad_value: -1
  train:
    total_shards: 1
    dist_world_size: 1
    iter_factory:
      _target_: espnet2.iterators.sequence_iter_factory.SequenceIterFactory
      shuffle: true
      collate_fn: ${dataloader.collate_fn}
      batches:
        # Prefer `sorted` when using RNN separators that pack sequences.
        type: unsorted
        shape_files:
          - ${stats_dir}/train/feats_shape
        batch_size: 4
        batch_bins: 4000000
  valid:
    total_shards: 1
    dist_world_size: 1
    iter_factory:
      _target_: espnet2.iterators.sequence_iter_factory.SequenceIterFactory
      shuffle: false
      collate_fn: ${dataloader.collate_fn}
      batches:
        type: ${dataloader.train.iter_factory.batches.type}
        shape_files:
          - ${stats_dir}/valid/feats_shape
        batch_size: ${dataloader.train.iter_factory.batches.batch_size}
        batch_bins: ${dataloader.train.iter_factory.batches.batch_bins}

##########################################################
#                    TRAINER SETTING                     #
##########################################################
trainer:
  accelerator: auto
  devices: ${num_device}
  num_nodes: ${num_nodes}
  accumulate_grad_batches: 1
  check_val_every_n_epoch: 1
  gradient_clip_val: 1.0
  log_every_n_steps: 100
  max_epochs: 100
  logger:
    - _target_: lightning.pytorch.loggers.TensorBoardLogger
      save_dir: ${exp_dir}/tensorboard
      name: tb_logger
  strategy: auto

fit: {}
