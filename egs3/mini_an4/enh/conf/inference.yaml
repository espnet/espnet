##########################################################
#             MINI_AN4 ENH INFERENCE OVERRIDES           #
##########################################################

dataset:
  test:
    - name: valid
      data_src_args:
        split: valid
        inference: true
    - name: test
      data_src_args:
        split: test
        inference: true

model:
  _target_: src.inference.SeparateSpeechWrapper
  train_config: ${exp_dir}/config.yaml
  model_file: ${exp_dir}/last.ckpt
  normalize_output_wav: true

output_fn: src.inference.build_output

runner:
  batch_size: 1
