system: asr
infer_config: infer.yaml

output_keys:
  text: hyp

inference:

pack:
  out_dir: demo

upload_demo:
  hf_username: ms180
  repo_name: minian4_asr_integration_test
  repo_type: space
  space_sdk: gradio
  # organization: your-org
