# Mini AN4 ASR inference config for ESPnet3 integration tests.

exp_dir: ${load_yaml:conf/train.yaml,exp_dir}
inference_dir: ${exp_dir}/inference
dataset_dir: ${load_yaml:conf/train.yaml,dataset_dir}

dataset:
  _target_: espnet3.components.data.data_organizer.DataOrganizer
  test:
    - name: test
      dataset:
        _target_: src.dataset.MiniAN4Dataset
        manifest_path: ${dataset_dir}/manifest/test.tsv

parallel:
  env: local
  n_workers: 1

model:
  _target_: espnet2.bin.asr_inference.Speech2Text
  asr_train_config: ${exp_dir}/config.yaml
  asr_model_file: ${exp_dir}/last.ckpt
  beam_size: 1
  ctc_weight: 0.3

input_key: speech
output_fn: src.inference.output_fn

provider:
  _target_: espnet3.systems.base.inference_provider.InferenceProvider
runner:
  _target_: espnet3.systems.base.inference_runner.InferenceRunner
