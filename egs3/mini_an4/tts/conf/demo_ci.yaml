system: base
infer_config: conf/inference_tacotron2.yaml

output_keys:
  audio: wav

inference:

pack:
  out_dir: demo

upload_demo:
  hf_username: ms180
  repo_name: minian4_tts_integration_test
  repo_type: space
  space_sdk: gradio
